[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn wsgi:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/api/allowed-schools"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
